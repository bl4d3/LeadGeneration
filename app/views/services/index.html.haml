.block
  .secondary-navigation
    %ul.wat-cf
      %li.first.active= link_to "#{t("web-app-theme.list", :default => "List")}", services_path
      %li= link_to "#{t("web-app-theme.new", :default => "New")}", new_service_path
  .content
    %h2.title
      = t("web-app-theme.all", :default => "All")
      Services
    .inner
      %table.table
        %tr
          %th.first ID
          %th
            = t("activerecord.attributes.service.category_id", :default => t("activerecord.labels.category_id", :default => "Category_id"))
          %th= t("web-app-theme.created_at", :default => "Created at")
          %th.last &nbsp;
        - @services.each do |service|
          %tr{:class => cycle("odd", "even")}
            %td
              = service.id
            %td
              = link_to service.category_id, service_path(service)
            %td
              = service.created_at
            %td.last
              = link_to "#{t("web-app-theme.show", :default => "Show")}", service_path(service)
              |
              \#{link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_service_path(service)} |
              \#{link_to "#{t("web-app-theme.delete", :default => "Delete")}", service_path(service), :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}"}
      .actions-bar.wat-cf
        .actions
- content_for :sidebar, render(:partial => 'sidebar')
