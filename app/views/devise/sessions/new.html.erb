<h1>Login Aziende</h1>

<p>Se sei un azienda effettua il login per usufruire dei servizi di Matrimonio Idee.</p>

<p>Non sei registrato? Cosa aspetti!?</p>

<p><strong>Diventa partner di Matrimonio Idee per ricevere preventivi da possibili clienti.</strong></p>

<%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
  <%= render :partial => "layouts/frontends/shared/form_errors", :locals => {:obj => @user} %>

	<div class="box-form">
		<div class="su-form">
	     <ol>
				<li class="nest_item">
				 	<span class="item">
						<%= f.label :email %>
						<%= f.email_field :email, :class => :text %>
					</span>
					<small>Inserisci il tuo username</small>
					<div class="clearF"></div>
				</li>

				<li class="nest_item">
				 	<span class="item">
						<%= f.label :password %>
						<%= f.password_field :password, :class => :text %>
					</span>
					<small>Inserisci la tua password</small>
					<div class="clearF"></div>
				</li>


				<div class="su-form-button">
					<%= f.submit "Login" %>
				</div>
			</ol>
		</div>
	</div>
<% end %>

<%= render :partial => "devise/shared/links" %>



